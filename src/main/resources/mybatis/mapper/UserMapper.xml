<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.boardver99.user.UserMapper">

    <insert id="insUser">
        Insert into t_user
        (uid,upw,nm,gender,addr)
        values
        (#{uid},#{upw},#{nm},#{gender},#{addr})
    </insert>
</mapper>